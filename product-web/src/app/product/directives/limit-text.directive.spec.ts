import { LimitTextDirective } from './limit-text.directive';

describe('LimitTextDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitTextDirective();
    expect(directive).toBeTruthy();
  });
});
